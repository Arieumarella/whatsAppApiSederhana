# .env - Puppeteer + server configuration
# NOTES:
# - Set HEADLESS=false to run Puppeteer in headful mode (show browser window).
# - If you want these variables loaded automatically, install `dotenv` and add
#   `require('dotenv').config();` at the top of `server.js`.
# - Alternatively, load this file into the PowerShell session before running Node.

# Puppeteer / Chrome settings
HEADLESS=your_value_here
PUPPETEER_DEVTOOLS=your_value_here
# Update this path if Chrome is installed elsewhere on your machine
PUPPETEER_EXECUTABLE_PATH=your_value_here
# Optional extra args (comma-separated)
PUPPETEER_ARGS=your_value_here

# Server settings
PORT=your_value_here
# Optional: comma-separated allowed origins for CORS. Leave empty to allow all.
ALLOWED_ORIGINS=your_value_here